import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function EditClientPage() {
  return (
    <PlaceholderPage
      title={pages.editClient.title}
      description={pages.editClient.description}
    />
  );
}
