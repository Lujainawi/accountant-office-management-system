import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function AddClientPage() {
  return (
    <PlaceholderPage
      title={pages.addClient.title}
      description={pages.addClient.description}
    />
  );
}
