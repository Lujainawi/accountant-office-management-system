import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function ClientDetailsPage() {
  return (
    <PlaceholderPage
      title={pages.clientDetails.title}
      description={pages.clientDetails.description}
    />
  );
}
