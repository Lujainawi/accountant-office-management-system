import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title={pages.dashboard.title}
      description={pages.dashboard.description}
    />
  );
}
