import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function TasksPage() {
  return (
    <PlaceholderPage title={pages.tasks.title} description={pages.tasks.description} />
  );
}
