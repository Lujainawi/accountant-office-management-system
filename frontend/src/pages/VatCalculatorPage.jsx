import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function VatCalculatorPage() {
  return (
    <PlaceholderPage
      title={pages.vatCalculator.title}
      description={pages.vatCalculator.description}
    />
  );
}
