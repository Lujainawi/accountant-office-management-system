import PlaceholderPage from "./PlaceholderPage";
import { pages } from "../content/he";

export default function SettingsPage() {
  return (
    <PlaceholderPage title={pages.settings.title} description={pages.settings.description} />
  );
}
